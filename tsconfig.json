{
  "compilerOptions": {
    "target": "ES6",                          // JavaScript 버전 설정 (ES6 이상 권장)
    "module": "ESNext",                     // 모듈 시스템 설정 (CommonJS 또는 ESNext)
    "strict": true,                           // 엄격한 타입 검사 옵션
    "esModuleInterop": true,                  // ES 모듈과 CommonJS 모듈 간 상호 운용성을 위한 옵션
    "skipLibCheck": true,                     // 라이브러리 타입 검사를 건너뜀
    "forceConsistentCasingInFileNames": true, // 파일 이름의 대소문자 일관성을 강제
    "moduleResolution": "Node",               // 모듈 해석 방식 설정 (Node 또는 Classic)
    "outDir": "./dist",                       // 컴파일된 파일이 저장될 디렉터리
    "rootDir": "./src"                        // 소스 파일의 루트 디렉터리
  },
  "include": ["src/**/*.ts"],                 // 포함할 파일 및 폴더
  "exclude": ["node_modules", "**/*.spec.ts"] // 제외할 파일 및 폴더
}
